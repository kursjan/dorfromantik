/**
 * Constants for {@link DebugRenderer} only. Deprecated with that class — HTML overlay uses CSS instead.
 *
 * @deprecated Migrate styling to the HTML debug overlay; remove when `DebugRenderer` is deleted.
 */
export const DEBUG_OVERLAY_TEXT_COLOR = 'black';

/** @deprecated See {@link DEBUG_OVERLAY_TEXT_COLOR}. */
export const DEBUG_OVERLAY_FONT = '20px Arial';
